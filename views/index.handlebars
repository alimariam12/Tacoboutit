
<section class="hero is-primary is-bold is-full">
  <div class="hero-body">
    <div class="container">
      <div class="columns">
        <div class="column is-four-fifths">
          <h1 class="animate__animated animate__backInRight">TACO 'BOUT IT</h1>
        </div>
        </div>
    </div>
  </div>
</section>
<h2 class="animate__animated animate__backInLeft">Add a Review</h2>

<form class="create-form">

  <div class="field">
    <div class="control">
      <div class="form-group">
        <input class="input is-transparent" type="text" placeholder="Review a Restaurant" id="ca" name="name">
      </div>
    </div>
  </div>

  <button type="submit" class="button is-primary is-small">Add Review</button>
</form><br>
<div class="container">
  <span id="rateMe3"  class="rating-faces"></span>
</div>

<br>

{{!-- <div class="columns">
  <div class="column is-half" <h2 class="is-size-5">Burgers I Want</h2> <br>

    <ul>
      {{#each tablename}}
      {{#unless category}}
      {{> tablename/partialsfile}}
      {{/unless}}
      {{/each}}
    </ul>
  </div> --}}
  {{!-- <div class="column is-half">
    <h2 class="is-size-5">Burgers I Have Eaten</h2><br>

    <ul>
      {{#each tablename}}
      {{#if category}}
      {{> tablename/partialsfile}}
      {{/if}}
      {{/each}}
    </ul>
  </div>
</div> --}}
<script src="/public/assets/js/index.js"></script>